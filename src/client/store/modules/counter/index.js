// My library
import actions from './actions'
import mutations from './mutations'

const state = {
  count: 0,
}

const getters = {}

const module = {
  state,
  getters,
  mutations,
  actions,
}

export default module
